# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
dist/
downloads/
eggs/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Environments
.env
.venv
venv/
ENV/
env/

# Databases
*.db
*.sqlite
*.sqlite3
# Excepción para el archivo de ejemplo si existe
!example.db

# Local Alembic config (database URL should be in .env)
alembic.ini
# Keep alembic directory structure but ignore generated files
alembic/versions/*.pyc
# You might want to keep the env.py and scripts directory under version control
# !alembic/env.py
# !alembic/script.py.mako

# IDE specific files
.vscode/
.idea/
*.iml
.project
.pydevproject
.settings/

# Credentials
*.json # Be careful if you need some json files versioned!
credentials*.json
*.pem
*.key
# Add specific credential files if not covered by above
# Assuming credentials folder is at backend/config/credentials
config/credentials/ee-*.json 

# Logs
logs/
*.log
*.log.*

# Jupyter Notebook
.ipynb_checkpoints

# Other
*.bak
*.swp
*~